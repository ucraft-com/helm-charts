{{- if .Values.service.enabled -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ucraft-v3.fullname" . }}-test-connection"
  labels:
    {{- include "ucraft-v3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ucraft-v3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: OnFailure
{{- end }}
