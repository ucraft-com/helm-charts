{{- if not .Values.app.envSecretName }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-nginx-config
data:
  nginx.conf: |
{{- tpl .Values.nginx.config . | nindent 4 }}
{{- end }}
